dev_inbox/
__pycache__/
*.egg-info/
.venv/
.pytest_cache/
*.world

# Runtime/built worlds (regenerable from examples/ source) + local agent state
worlds/
.claude/
# Transport state + chat logs live under worlds/.construct/ (covered by worlds/
# above); listed explicitly so it's clear secrets/transcripts never get committed.
worlds/.construct/
# Secrets (e.g. CONSTRUCT_TELEGRAM_TOKEN) — never committed.
.env

# Generated hidden bibles — runtime artifacts (per-user, the authoring side of
# the firewall), distinct from the committed examples/ fixture (Kernos 063 B.2)
generated/
